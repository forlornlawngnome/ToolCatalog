<br />
<%=render "tool_container", :tool=>@tool%>

<%if current_user.is_admin || current_user.is_student_admin%>
	<h3>Recent Usage</h3>
	<hr />
	<%@tool.tool_logs.order("time_beginning DESC").limit(20).each do |log|%>
		<li><%=link_to log.person.name, log.person%>: <%=local_time log.time_beginning%> - <%=local_time log.time_ending rescue ""%></li>
	<%end%>
<%end%>

<%= link_to 'Back', back_link, :class=>"btn btn-danger pull-right" %>
